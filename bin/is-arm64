#!/bin/sh

# Returns 0 if the current machine is arm64 (Apple Silicon), 1 otherwise.
# POSIX sh so the Makefile can invoke this at parse time on any host.

[ "$(uname -m)" = "arm64" ] && exit 0 || exit 1
