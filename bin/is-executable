#!/bin/sh

# Returns 0 if the given command is executable, 1 otherwise.
# POSIX sh so the Makefile can invoke this at parse time on any host.

if command -v "$1" >/dev/null 2>&1; then
    exit 0
else
    exit 1
fi
