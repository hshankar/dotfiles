#!/bin/sh

# Returns 0 if the current OS is macOS, 1 if it is not.
# Uses uname -s (POSIX) so this works on hosts that don't have zsh yet —
# the Makefile invokes it at parse time via $(shell ...).

case "$(uname -s)" in
    Darwin) exit 0 ;;
    *) exit 1 ;;
esac
